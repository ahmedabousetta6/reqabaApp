import 'package:flutter/material.dart';

class branch_data extends StatefulWidget {
  @override
  _branch_dataState createState() => _branch_dataState();
}

class _branch_dataState extends State<branch_data> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
      body: Container(
        color: Colors.grey,

      ),
    );
  }
}
